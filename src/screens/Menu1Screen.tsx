import { ScreenWrapper } from '@/shared/ui';
import { Text } from 'react-native';

export const Menu1Screen: React.FC = () => {
  return (
    <ScreenWrapper>
      <Text>Menu1</Text>
    </ScreenWrapper>
  );
};
